export const writeTestRestaurant = async () => {
  return { success: true, message: 'Test restaurant write completed' };
};

export const writeMultipleTestRestaurants = async () => {
  return { success: true, message: 'Multiple test restaurants write completed' };
};
